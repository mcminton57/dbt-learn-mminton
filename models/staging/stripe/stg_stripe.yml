version: 2

models:
  - name: stg_stripe__payments
    description: This model cleans up payments data
    columns:
      - name: payment_id
        description: Primary key
        tests:
          - unique
          - not_null